apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-policy
  namespace: {{ .Values.namespaces.vault }}
data:
  student-api-policy.hcl: |-
    path "{{ .Values.vault.secretPath }}" {
      capabilities = ["read"]
    }
